# Copy this file to `.env.git` and fill in your token.
# `.env.git` is gitignored — never commit it.
#
# Create a token at: https://github.com/settings/tokens
# Scopes needed: `repo` (or fine-grained: Contents=Read/Write on this repo).

GITHUB_TOKEN=ghp_replace_me_with_your_personal_access_token
